---
- hosts: proxypool
  tasks:
    - import_role:
        name: docker_update_image
      vars:
        image: "{{ haproxy_image }}"
      tags:
        - haproxy

    - import_role:
        name: docker_update_image
      vars:
        image: "{{ tor_image }}"
      tags:
        - tor

    - import_role:
        name: docker_update_image
      vars:
        image: "{{ vanguards_image }}"
      tags:
        - vanguards
